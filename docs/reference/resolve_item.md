# Resolve Human-Readable Item Name to Integer Index safely via Mapped Parameters

Resolve Human-Readable Item Name to Integer Index safely via Mapped
Parameters

## Usage

``` r
resolve_item(results, item)
```
