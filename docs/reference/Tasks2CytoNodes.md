# Push Standalone Task Nodes to Cytoscape

Pushes an orphaned list of task nodes directly to Cytoscape without
edges.

## Usage

``` r
Tasks2CytoNodes(task_names, compute = "dina")
```

## Arguments

- task_names:

  Character vector of task node IDs.

- compute:

  Character. Default compute rule. Default is "dina".
