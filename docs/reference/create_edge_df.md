# Create a Standardized Edge DataFrame

Helper function to generate a well-formatted edge `data.frame`.

## Usage

``` r
create_edge_df(from, to)
```

## Arguments

- from:

  Character vector of source node identifiers.

- to:

  Character vector of target node identifiers.

## Value

A `data.frame` formatted for graph construction.
